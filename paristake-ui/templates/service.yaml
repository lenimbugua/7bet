apiVersion: v1
kind: Service
metadata:
  name: {{ include "paristake-ui.fullname" . }}
  labels:
    {{- include "paristake-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "paristake-ui.selectorLabels" . | nindent 4 }}
