apiVersion: v1
kind: Service
metadata:
  name: {{ include "bet-ug-ui.fullname" . }}
  labels:
    {{- include "bet-ug-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bet-ug-ui.selectorLabels" . | nindent 4 }}
